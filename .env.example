# ===========================================================================
#  Rhizoo Quant — Environment Configuration
# ===========================================================================
#
#  HOW IT WORKS (two-layer .env system):
#
#    1. ROOT .env  (this level — rhizoo-quant/.env)
#       Contains shared credentials and global settings inherited by ALL bots.
#       python-dotenv walks up the directory tree, so every bot automatically
#       picks up variables defined here.
#
#    2. BOT-LEVEL .env  (e.g. bots/rhizoo-alpha-bot/.env)
#       Optional overrides specific to a single bot. Any variable set here
#       takes priority over the root .env. Use this for bot-specific tuning
#       like thresholds, paper trading toggles, or per-bot balances.
#
#  SETUP:
#    1. Copy this file to .env      →  cp .env.example .env
#    2. Fill in your API credentials below.
#    3. Optionally create a bot-level .env to override specific values.
#       (Only set what you want to change — everything else inherits from root.)
#
# ===========================================================================

# ---------------------------------------------------------------------------
# Exchange API Credentials (shared across all bots)
# ---------------------------------------------------------------------------

# Binance
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_SECRET=your_binance_secret_here

# Bybit (for future bots)
BYBIT_API_KEY=your_bybit_api_key_here
BYBIT_SECRET=your_bybit_secret_here

# ---------------------------------------------------------------------------
# Global Settings
# ---------------------------------------------------------------------------

LOG_LEVEL=DEBUG

# ---------------------------------------------------------------------------
# Bot-Specific Settings (override in bot-level .env if needed)
# ---------------------------------------------------------------------------

# Risk Manager
ACCOUNT_BALANCE=10000.0          # Account equity in quote currency (USDT)

# Imbalance Engine
ZSCORE_THRESHOLD=2.0             # Volume Z-Score threshold for signal detection

# Paper Trading
PAPER_TRADING=true               # Set to "true" to enable simulated execution
PAPER_BALANCE=10000.0            # Virtual starting balance for paper trades

# ---------------------------------------------------------------------------
# Redis Telemetry (optional — bot runs fine without it)
# ---------------------------------------------------------------------------
#
# The bot broadcasts real-time events (pulses, signals, trades) to a Redis
# Pub/Sub channel. External services (dashboards, alerting, logging) can
# subscribe to receive these events.
#
# OPTION A: Local Redis (default)
#   Install:  sudo apt install redis-server   (Ubuntu/Debian)
#             brew install redis               (macOS)
#   Start:    redis-server
#   Config:   Leave defaults below as-is.
#
# OPTION B: Hosted Redis with SSL/TLS (e.g. Upstash, Redis Cloud, AWS ElastiCache)
#   Set REDIS_HOST to your provider's hostname.
#   Set REDIS_PORT to your provider's port (often 6380 for TLS).
#   Set REDIS_PASSWORD to the auth token / password.
#   Set REDIS_SSL=true to enable TLS.
#
# If Redis is not running or not configured, the bot simply skips telemetry.

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=                  # Leave empty for no auth (local dev)
REDIS_SSL=false                  # Set to "true" for hosted Redis with TLS
